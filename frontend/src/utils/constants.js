// Centralized constants so route paths and storage keys are defined in one place.

export const ROUTES = {
  LOGIN: '/login',
  DASHBOARD: '/dashboard',
};

export const STORAGE_KEYS = {
  TOKEN: 'tms.auth.token',
  USERNAME: 'tms.auth.username',
};

// Max tasks shown per page on the dashboard.
export const TASKS_PER_PAGE = 7;
